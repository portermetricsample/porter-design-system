Porter pill label — IBM Plex Mono, uppercase, tracked. Functional tags only (status, category, "new") — never decoration.

```tsx
<Badge variant="aqua">New</Badge>
```

Variants: `purple` · `pink` · `aqua` · `yellow` · `dark`. Keep to 1–3 words. This is the only place a "pill" shape is allowed in the system.
